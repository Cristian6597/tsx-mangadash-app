import { db } from "@db";
import * as schema from "@shared/schema";
// This file will be used to implement data storage when you add a backend

// Placeholder function since we'll be handling data on the frontend with db.json
export const storage = {
  // Add functions here when implementing the backend
};
